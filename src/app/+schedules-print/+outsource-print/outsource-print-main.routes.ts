import {
    Routes
} from '@angular/router';

import {
    OutsourcePrintMainComponent
} from './outsource-print-main.component';

export const routes: Routes = [
    {
        path: '', component: OutsourcePrintMainComponent
    }
];
